import { Item, Span } from "react-html-email";

const BodyFooter = (props: any) => (
  <Item style={props.styles.item}>
    <Span color={props.styles.span}>
      {" "}
    </Span>
  </Item>
);

export default BodyFooter;
