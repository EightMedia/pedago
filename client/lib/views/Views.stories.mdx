import { Meta } from "@storybook/addon-docs";

<Meta
  title="Views/Introduction"
  parameters={{
    viewMode: "docs",
    previewTabs: {
      canvas: { hidden: true },
    },
  }}
/>

# Views

Views are full page layouts. The backend sends the user to a view, the user interacts with the view and in the end emits a result.
