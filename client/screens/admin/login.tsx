const ContentPage = () => {
    const thing = ['ding', 'dong'];
    return (
        <div>
            <h1>Admin</h1>
            <p>{thing[0]}</p>
        </div>
    );
};
export default ContentPage;
